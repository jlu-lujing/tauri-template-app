[workspace]
resolver = "2"
members = ["src-tauri"]
default-members = ["src-tauri"]

# ─── Profile: shared for release builds ────────────────────────────────
[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
